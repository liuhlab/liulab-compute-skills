"""The Edison platform spend path: preflight, personas, projects, Kosmos, tasks, datasets.

Deliberately empty of imports. `edison_cli.preflight` is a security control that
`tests/lint.sh` runs with a bare interpreter, and anything imported here would be imported
on the way to it.
"""
